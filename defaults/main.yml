---
# The SMB host with path that contains your roms
smb_host_path: '' 

# SMB credentials
smb_username: ''
smb_password: ''

# Whether to use SMB V3
smb_v3: false

# Mapping of roms from EmuELEC rom dir to SMB rom dir
rom_map: []
# - { rom_dir: 'snes', smb_dir: 'SNES'}
# - { rom_dir: 'nes', smb_dir: 'NES'}
# - { rom_dir: 'dreamcast', smb_dir: 'Dreamcast'}
# - { rom_dir: 'psx', smb_dir: 'PSX'}
# - { rom_dir: 'megadrive', smb_dir: 'Sega Mega Drive'}
